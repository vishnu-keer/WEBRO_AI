/**
 * Agent bootstrap. Import this once (worker, dashboard) to register every agent
 * and its job handler. Add future agents here as they are built.
 */
import "./website-audit/run";
import "./seo/run";
