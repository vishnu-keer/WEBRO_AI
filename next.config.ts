import type { NextConfig } from "next";

/**
 * Next.js 16 configuration. Kept minimal on purpose — Turbopack is the default
 * bundler, Server Actions and the App Router work out of the box, so there are no
 * flags to set here yet. Add options as real needs appear.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
